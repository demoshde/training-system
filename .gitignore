# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build output
client/dist/
client/build/

# Environment files
.env
server/.env
client/.env
.env.local
.env.*.local

# Keep production env templates (no real secrets)
!.env.docker
!.env.production

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# Uploads (user-generated content, not tracked in git)
server/uploads/images/*
server/uploads/pdfs/*
server/uploads/presentations/*
!server/uploads/images/.gitkeep
!server/uploads/pdfs/.gitkeep
!server/uploads/presentations/.gitkeep

# Backups
backups/

# Package locks (optional — remove these lines if you want to track them)
# package-lock.json
