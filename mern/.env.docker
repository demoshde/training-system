# Docker Environment Variables for Training System
# Copy this to .env or use: docker compose --env-file .env.docker up

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-jwt-secret-key-change-in-production
ADMIN_JWT_SECRET=your-admin-jwt-secret-key-change-in-production

# MongoDB (uses service name in docker-compose)
MONGODB_URI=mongodb://mongo:27017/training_system

# Server
PORT=5001
HOST=0.0.0.0
